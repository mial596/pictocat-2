import { CatImage } from './types';

// The master image catalog has been moved to the database.
// This file is now empty but kept for potential future frontend-specific initial data.

export const ALL_IMAGES_FLAT: CatImage[] = [];

// Defines which images are unlocked at the very beginning of the game.
export const INITIAL_UNLOCKED_IMAGE_IDS: number[] = [];
